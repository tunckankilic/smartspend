# Screenshots

Drop the App Store / README screenshots here. The README references these
filenames — keep the names so the image links resolve once the PNGs are added.

Required (referenced from the project README):

| File | Shows |
|---|---|
| `01-scan.png` | Receipt camera + OCR capture |
| `02-parse-result.png` | Editable parse result (store, items, total) |
| `03-dashboard.png` | Dashboard charts + insights |
| `04-expenses.png` | Expense list / detail |
| `05-budget.png` | Budget caps + progress rings |
| `06-receipt-archive.png` | Receipt archive + warranty reminder |

App Store store-listing assets (see `docs/internal/appstore/`):

- 6.7"/6.5" device frames (1290×2796 or 1242×2688)
- 5.5" device frames (1242×2208)
- 1024×1024 app icon (no alpha, no rounded corners)

Capture on a clean device/simulator with seeded demo data. Use the same locale
across a set (one full set per language is ideal: TR / EN / DE).
